/**
 * kouchao 创建于 2018/8/27
 */

import LayFooter from "./src/footer";

/* istanbul ignore next */
LayFooter.install = function(Vue) {
  Vue.component(LayFooter.name, LayFooter);
};

export default LayFooter;
